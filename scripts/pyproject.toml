[project]
name = "sunny-pint"
version = "0.1.0"
description = "Find sunny beer garden seats — real-time shadow maps for UK pubs"
requires-python = ">=3.11"
dependencies = [
    "rasterio>=1.4",
    "numpy>=2.0",
    "pyproj>=3.6",
    "fiona>=1.10.1",
    "shapely>=2.1.2",
    "osmium>=4.3.1",
]
